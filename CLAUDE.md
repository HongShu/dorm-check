# CLAUDE.md

> 本文件是给 Claude Code 的协作指南。Claude Code 启动时会自动读取本文件。
> 产品需求请见 `docs/查寝管理系统-需求说明.md`，本文件**只讲工程协作约定**，不重复需求内容。

---

## 1. 项目速览

**项目名**：dorm-check（学生查寝管理系统）

**目标**：宿管手机端极简录入 + 管理员端 LLM 简报。1000+ 学生规模。

**技术栈**（强约束，不要替换）：
- 后端：Python 3.10+ / FastAPI / SQLAlchemy 2.0 / SQLite
- 前端：微信小程序原生框架
- LLM：Anthropic Claude API（claude-haiku-4-5）

**当前阶段**：[请在每次开发开始时手动更新这一行]
- [ ] 阶段一：后端 + 数据
- [ ] 阶段二：宿管端小程序
- [ ] 阶段三：管理员端 + 联调

---

## 2. 必读文档

每次开始新任务前，先确认你已经读过：

1. `docs/查寝管理系统-需求说明.md`——所有功能需求
2. 本文件（CLAUDE.md）——工程约定
3. 当前阶段对应的代码目录（如开发后端时先看 `backend/`）

**遇到不清楚的需求**：去需求文档第四节按功能编号（F-01 到 F-12）查找；如仍不清楚，停下来问我，不要自行假设。

---

## 3. 工作流程

### 3.1 任务粒度

- **一次只做一件事**。如果我说"做完阶段一"，请你拆成多个子任务依次确认，而不是一次产出全部代码。
- **每个子任务完成后等我确认**，不要自动进入下一个。
- 单次回复中产出的代码量建议**不超过 300 行**，超过就拆。

### 3.2 修改前先理解

- 修改任何已有文件前，**先用 `view` 看一遍当前内容**，不要凭记忆改。
- 涉及多文件改动时，**先列出改动计划**，等我确认再动手。

### 3.3 提交前检查清单

每完成一个功能模块，自检以下事项：

- [ ] 代码能跑通（至少手动测试主路径）
- [ ] 没有 print 调试语句残留
- [ ] 没有 hardcode 的密钥、token、测试数据
- [ ] 新增的依赖已加入 `requirements.txt`
- [ ] 涉及数据模型变更的，提示我执行迁移

### 3.4 不要做的事

- ❌ **不要主动删除**我已有的代码或文件，除非我明确要求
- ❌ **不要重构无关代码**，即使你觉得"顺手优化一下更好"
- ❌ **不要修改 `.env` 文件**，只能修改 `.env.example`
- ❌ **不要执行** `rm -rf`、`git push --force`、删除数据库文件等破坏性命令
- ❌ **不要为了"更专业"自行引入新技术**（Redis、Celery、Docker Compose 等）

---

## 4. 代码规范

### 4.1 Python (后端)

- **格式化**：遵循 PEP 8，行宽 100 字符
- **类型注解**：所有函数签名必须带类型注解
- **命名**：
  - 文件名 / 函数名：`snake_case`
  - 类名：`PascalCase`
  - 常量：`UPPER_SNAKE_CASE`
- **导入顺序**：标准库 → 第三方 → 本项目（用空行隔开）
- **异常处理**：不要用裸 `except:`，必须指定异常类型
- **日志**：用 Python `logging` 模块，不要用 `print`

### 4.2 SQLAlchemy 模型

- 用 SQLAlchemy 2.0 的 `Mapped` 风格写法，不要用旧的 `Column` 风格
- 表名用单数形式：`student`、`check_record`、`user`
- 所有时间字段用 `DateTime(timezone=True)`，存 UTC

### 4.3 FastAPI 路由

- 路径用复数：`/api/students`、`/api/check-records`
- 响应统一格式（见需求文档 6.4 节）
- 用 Pydantic schema 定义请求/响应，**不要直接返回 ORM 对象**
- 路由函数尽量薄，业务逻辑放在 `services/`

### 4.4 小程序

- 页面命名：`pages/<功能>-<页面>/index`，如 `pages/checker-rooms/index`
- API 调用统一走 `utils/api.js`，不要在页面里直接 `wx.request`
- 不要使用 npm 包，除非必要（小程序对 npm 支持不完美）

---

## 5. 常用命令

### 后端

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（首次）
python -m venv .venv
source .venv/bin/activate    # Windows: .venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 启动开发服务器
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# 运行测试
pytest tests/ -v

# 数据库初始化（首次或 schema 变更后）
python -m scripts.init_db

# 导入学生 Excel
python -m scripts.import_students data/students.xlsx
```

### 小程序

- 用微信开发者工具打开 `miniprogram/` 目录
- 后端地址在 `miniprogram/utils/config.js` 中配置

### Git

```bash
# 提交规范
git commit -m "feat(checker): 实现单寝室查寝提交接口"
git commit -m "fix(admin): 修复简报生成超时问题"

# 类型: feat / fix / docs / refactor / test / chore
# 范围: checker / admin / auth / llm / db / ...
```

**注意**：除非我明确要求，**不要自动 commit**。修改完代码让我自己 review 后再决定提交。

---

## 6. 关键约束（红线）

这些是我反复强调过的、**绝对不要触碰**的边界：

1. **异常判定永远是代码规则**（见需求文档 3.4 节），LLM **只**负责生成简报文本
2. **SQLite 不要换成其他数据库**，1000 人规模够用
3. **小程序不要换成 H5/React/Vue**
4. **不要主动加功能**：消息推送、用户头像、统计图表、请假流程、心情记录——见需求文档第九节
5. **LLM 调用必须有兜底**（见需求文档 F-10），失败时返回原始结构化数据
6. **学生表的字段名严格按需求文档 3.1 节**，不要因为"更优雅"而改名（如 `student_id` 不要改成 `sid`）

---

## 7. 上下文信息

### 7.1 业务上下文

- **宿管的真实工作场景**：晚上 21:00-22:30 之间挨个寝室查，时间紧、手忙
- **管理员的真实需求**：21:30 左右希望能在 1 分钟内看完今晚所有异常
- **学生在校状态由辅导员在另一套系统维护**，本系统**不负责状态变更**
- **第一版用户极少**（5 个宿管 + 几个管理员），不需要考虑高并发

### 7.2 已知数据特征（来自实际表格）

- 楼栋编号：7、8、10、11、17、19（**不连续**，不要假设是 1-N）
- 寝室号：3 位数字，如 "108"、"203"、"518"（**用 TEXT 存，不要用 INTEGER**）
- 学号：7 位数字字符串
- 男女混住于不同楼栋（按性别分配）
- 辅导员人数：5 人左右

### 7.3 LLM 调用注意事项

- 只在**简报生成**这一个场景调用 LLM
- 模型固定 `claude-haiku-4-5`，不要换成其他模型
- prompt 模板见需求文档第五节，**直接复制使用**
- API key 通过环境变量 `ANTHROPIC_API_KEY` 读取
- 必须设超时（建议 10 秒），超时走兜底分支

---

## 8. 与我（用户）的协作偏好

- **不确定时停下来问，不要自行假设**
- **重大决策列选项让我选**，不要直接做主
- **回复尽量简洁**，不要每次都重复"我理解了你的需求"这种铺垫
- **代码注释用中文**（变量名英文）
- **错误信息友好**，不要给用户看堆栈
- **不要谄媚**，发现我说的有问题直接指出

---

## 9. 本文件的维护

- 每发现一个新的协作约定（比如"以后所有时间字段都用 UTC"），追加到对应章节
- 每完成一个开发阶段，更新第 1 节"当前阶段"
- 本文件**不要**被 Claude Code 自动修改，所有变更由我手动写入
